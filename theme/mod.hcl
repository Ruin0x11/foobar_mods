mod {
    name = "theme"
    author = "Ruin0x11"
    version = "0.0.1"
    description = "Adds a generic framework for asset transformation, which can be used for non-intrusive UI theming."

    # TODO remove
    dependencies = ["core"]
}
